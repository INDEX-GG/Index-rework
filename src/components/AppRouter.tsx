import React from "react";
import { Routes, Route } from "react-router-dom";

const AppRouter = () => {
  return (
    <Routes>
      <Route />
    </Routes>
  );
};

export default React.memo(AppRouter);
